import { Mapbox } from './Mapbox';
import { MappedIn } from './MappedIn';

export const Main = () => {
    return (
        // <Mapbox style={{overflow: 'hidden'}}></Mapbox>
        <MappedIn></MappedIn>
    )
}